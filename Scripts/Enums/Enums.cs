public enum InventoryLocation
{
    player, // 0
    chest, // 1
    count // 2
}

public enum ToolEffect
{
    none,
    watering
}

public enum Direction
{
    up,
    down,
    left,
    right,
    none
}

public enum ItemType
{
    Seed, // tohum
    Commodity, // eşya
    Watering_tool,
    Hoeing_tool, // çapa
    Chopping_tool, // doğrama
    Breaking_tool, // kırma
    Reaping_tool, // hasat
    Collecting_tool, // toplama
    Reapable_scenary, // kesilebilir şeyler (çim)
    Furniture,
    none,
    count
}

